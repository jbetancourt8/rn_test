import { StyleSheet } from 'react-native';
 
const styles = StyleSheet.create({
  container: {
    height: 50,
    width: '80%',
    padding: 10,
    borderColor: 'grey',
    borderWidth: 1,
    borderRadius: 10
  }
})

export default styles; 